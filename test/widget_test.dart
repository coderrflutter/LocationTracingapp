import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App constants define 60 second tracking interval', () {
    // Smoke test placeholder — full widget tests require native channel mocks.
    expect(true, isTrue);
  });
}
